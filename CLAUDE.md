# راهنمای کدنویسی پروژه

این قواعد در **همهٔ فازها** رعایت می‌شوند.

---

## ۱. سقف اندازهٔ فایل: ۱۵۰ خط

هر فایل حداکثر ۱۵۰ خط. اگر از این عبور کرد، یعنی بیش از یک مسئولیت دارد
و باید شکسته شود.

### استثناهای مجاز (فقط این‌ها)

| فایل | دلیل |
|---|---|
| `prisma/schema.prisma` | روابط بین جداول باید یکجا دیده شوند؛ چندفایلی‌بودن در Prisma هنوز preview است |
| فایل‌های تست | یک فایل تست کنار فایل کدِ خودش می‌ماند حتی اگر بلند شود |

هر استثنای جدید باید در همین جدول ثبت و دلیلش نوشته شود.

### چطور بشکنیم (به ترتیب اولویت)

1. **بر اساس مسئولیت، نه اندازه.** فایل ۱۶۰ خطی که یک کار منسجم می‌کند،
   بهتر از دو فایل ۸۰ خطیِ به‌زور جداشده است. اول بپرسید «این فایل چند
   کار می‌کند؟».
2. **داده را از منطق جدا کنید.** جدول‌های ثابت (مثل تعریف اقلام) در فایل
   جدا از توابعی که رویشان کار می‌کنند.
3. **در کامپوننت React، منطق را به hook ببرید.** کامپوننت فقط ظاهر؛
   محاسبه و state در `use*.ts` کنارش.

---

## ۲. ساختار پوشه‌ها

```
packages/shared/src/
  domain/
    transactions/          هر مفهوم یک پوشه
      types.ts             شناسه‌ها و enum
      definitions/         جدول داده، تکه‌شده بر اساس معنا
      index.ts             نقطهٔ ورود عمومی (barrel)
    calculation/
      engine.ts            منطق محاسبه
      types.ts             نوع ورودی/خروجی

apps/web/src/
  components/ui/           کامپوننت‌های عمومی و بی‌طرف
    NumberInput/
      index.tsx            فقط ظاهر
      useNumberInput.ts    منطق
  features/<نام فیچر>/     کد مخصوص یک فیچر
    components/
    hooks/
    api.ts

apps/api/src/
  modules/<نام ماژول>/
    dto/                   هر DTO یک فایل
    <name>.controller.ts   فقط مسیر و اعتبارسنجی ورودی
    <name>.service.ts      منطق کسب‌وکار
```

**قاعدهٔ barrel:** هر پوشه یک `index.ts` دارد که فقط `export` می‌کند و
هیچ منطقی داخلش نیست. بیرون از پوشه همیشه از `index` وارد می‌شوند، نه از
فایل داخلی — تا تغییر ساختار داخلی، بیرون را نشکند.

---

## ۳. قواعد کد

- **TypeScript سختگیرانه:** `strict` و `noUncheckedIndexedAccess` روشن‌اند.
  هرگز با `any` یا `as` از خطا فرار نکنید؛ خطا را واقعاً حل کنید.
- **مبالغ همیشه `bigint`** و به ریال صحیح. هرگز ممیز شناور.
- **تاریخ:** ذخیره و انتقال به‌صورت ISO میلادی؛ تبدیل به جلالی فقط در نمایش.
- **کامنت فارسی** و توضیح‌دهندهٔ «چرا»، نه «چه». کدی که چه می‌کند از خودش
  پیداست؛ چرایی تصمیم نیست.
- **بدون تکرار منطق:** هر قاعدهٔ کسب‌وکاری فقط یک‌بار، در `packages/shared`،
  تا بک‌اند و فرانت‌اند واگرا نشوند.

---

## ۴. پیش از هر کامیت

```bash
npm run typecheck && npm test && npm run lint
```

هر سه باید سبز باشند.

---

## ۵. طراحی رابط کاربری

| موضوع | تصمیم |
|---|---|
| رنگ اصلی | سرمه‌ای / آبی نفتی |
| تراز | سبز · مازاد: آبی · کسری: قرمز |
| فونت | Vazirmatn (محلی، نه CDN) |
| جهت | RTL کامل |

- **رنگ هرگز تنها حامل معنا نیست** — کنار هر نشان وضعیت، متن فارسی هم بیاید.
- **اعداد مالی** با کلاس `financial-figure` (عرض ثابت) تا ستون‌های جدول
  در هم نریزند.
- **چیدمان صفحهٔ ثبت تراکنش:** دوستونی — راست: اقلام مانده صندوق،
  چپ: اقلام جمع اسناد — با باکس خلاصهٔ چسبان در پایین صفحه.
