/**
 * نقطهٔ ورود عمومی مفهوم «تراکنش».
 *
 * بیرون از این پوشه همیشه از همین فایل وارد شوید، نه از فایل‌های داخلی —
 * تا تغییر ساختار داخلی، مصرف‌کننده‌ها را نشکند.
 */

export * from './types.js';
export * from './registry.js';
