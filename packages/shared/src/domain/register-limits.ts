/**
 * محدودیت‌های عددی صندوق روزانه.
 *
 * اینجا زندگی می‌کنند تا بک‌اند و فرانت‌اند دقیقاً یک عدد را بشناسند؛
 * وگرنه فرم چیزی را پیشنهاد می‌دهد که سرور ردش می‌کند.
 */

/**
 * حداکثر چند روز عقب‌تر از امروز می‌توان صندوق ساخت.
 *
 * تعطیلی اداری چندروزه در حالی که فروشگاه باز است، حالت واقعی و رایجی
 * است. سقف یک‌روزه باعث می‌شد فروش روزهای جامانده برای همیشه
 * ثبت‌نشدنی بماند و از گزارش‌ها بیفتد.
 */
export const MAX_BACKDATE_DAYS = 7;

/**
 * حداکثر تصویر برای هر ردیف.
 *
 * سند (بند ۸.۳) نمونهٔ «۵ چک از یک مشتری» را می‌آورد، ولی تعداد واقعی
 * می‌تواند بیشتر باشد. این سقف فقط دفاعی است تا آپلود انبوه، فضای
 * ذخیره‌سازی را پر نکند.
 */
export const MAX_IMAGES_PER_ROW = 15;
